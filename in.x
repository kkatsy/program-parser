float Foo(int val);

void main(){
	float Value;
    
	Value = Foo(7 + 2);
}

float Foo(int val){

    if(val > 0){
        TestFloat = banana = another_banana = TestFloat * TestFloat;
        val = val - 1;   
    }

    float TestFloat = -4 / potato * (2 + 2);

    return TestFloat;
}
